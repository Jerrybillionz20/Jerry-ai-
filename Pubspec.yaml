name: jerry_ai
description: Jerry AI Cross Platform App
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true  
  dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
dependencies:
  flutter_dotenv: ^5.0.2
